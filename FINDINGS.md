# 知見

ブラウザ別の再現状況は [BROWSER_SUPPORT.md](BROWSER_SUPPORT.md) に分離して記録する。

効いたことと同じだけ、効かなかったことを残す。ここが最終的にこのリポジトリの中身になる。

記録の形式は固定する。

- **主張** — 一文で言い切る
- **根拠** — どの実験で確かめたか
- **確度** — `実測` / `仕様から確定` / `仮説`
- **効き方** — この知見が実際に何を変えるか

---

## F001 CSS アニメーションは `currentTime` の代入だけで外部から完全に駆動できる

- **主張** — `animation-play-state: paused` にした上で `element.getAnimations()` の `currentTime` へ代入すると、CSS アニメーションを任意の進行度に固定できる。
- **根拠** — `lab/lab.js` の時計。18 実験を同時に同位相で駆動して確認。
- **確度** — 実測（Chromium）
- **効き方** — 「同位相での比較」「任意時点での固定」「静止画の連番への展開」が別々の仕掛けではなく 1 つの仕組みで手に入る。アニメーションを比較検討する道具を作るときの土台になる。

## F002 DOM 挿入直後は `getAnimations()` が空を返す

- **主張** — 要素を挿入した直後はスタイル解決が走っておらず、CSS アニメーションがまだ生成されていない。
- **根拠** — `lab/strip.html` の初期実装。挿入直後に `getAnimations()` を呼んで 0 件が返り、全コマが `from` の状態で固まった。
- **確度** — 実測
- **効き方** — 生成した直後にアニメーションへ触る処理は、2 フレーム待つ必要がある。無言で「全部同じ絵」になるため気づきにくい。

## F003 `offset-path: path()` は箱の大きさに追従しない

- **主張** — `path()` の座標は絶対値なので、参照ボックスが小さい場面では要素が外へ出て消える。基本図形（`ellipse()` / `circle()` / `rect()`）なら `%` で書けて追従する。
- **根拠** — `experiments/orbit`。150px の舞台向けに書いた `path()` が、118px のフィルムストリップでは完全に画面外へ出た。`ellipse(30% 26%)` に変えて両方で成立。
- **確度** — 実測
- **効き方** — モーションパスを再利用可能なパーツとして配るなら `path()` は使えない。基本図形で書けるかを先に検討する。

## F004 ease-out 系は前半 3 割で動きが終わる

- **主張** — `cubic-bezier(0.22, 1, 0.36, 1)` では、時間の 25% 時点で出力が 78% に達する。等間隔でコマを採ると後半が全部同じ絵になる。
- **根拠** — `lab/strip.html` の均等サンプリングと前半密サンプリングの比較。
- **確度** — 実測
- **効き方** — アニメーションを静止画で検討するときは、実時間ではなく前半に密なサンプリングを採る。逆に言えば、ease-out の体感時間は指定値よりずっと短い。指定 600ms は体感 200ms 前後として設計すべき。

## F005 `box-shadow` による elevation は基準面の明度を前提にする

- **主張** — 影だけで高さを表現する手法は、下地が明るいことに依存している。暗色背景では影が背景に沈んで何も起きていないように見える。
- **根拠** — `experiments/lift`。暗色の舞台では不可視。舞台を明色にして初めて読めた。
- **確度** — 実測
- **効き方** — ダークテーマで elevation を表現するには影ではなく別の手段（面の明度差、縁の発光）が要る。影を汎用パーツとして配るのは危険。

## F006 `skew` は個別プロパティを持たないため他の変形と合成できない

- **主張** — `translate` / `rotate` / `scale` は個別プロパティがあり別々のアニメーションから同時に動かせるが、`skew` は `transform` を占有するため、他の変形と組み合わせると衝突する。
- **根拠** — `experiments/skew`。CSS Transforms Level 2 の仕様。
- **確度** — 仕様から確定
- **効き方** — 原子を組み合わせて分子を作る設計において、`skew` だけが合成できない例外になる。L2 以降では `skew` を含む組み合わせを別扱いにする必要がある。

## F007 `letter-spacing` を中央寄せで動かすと片側の文字しか動かない

- **主張** — `letter-spacing` は最後の文字の後ろにも余白を足す。そのため中央寄せのテキストでは末尾側だけが押し出され、末尾の文字が固定されたまま先頭の文字だけが動いて見える。
- **根拠** — `experiments/track`。"Aa" の各文字の左端を実測。修正前は `a` が 937.5px から一切動かず、`A` だけが 8.2px 動いた。同量の負のマージンで末尾余白を打ち消すと、`A` が +4.5px、`a` が −4.5px と左右対称になった。
- **確度** — 実測
- **効き方** — 字間を広げる演出は「中央から開く」つもりで書くと必ず片寄る。打ち消しには動いている値を参照する必要があるため、`@property` で `<length>` として型を付け、`letter-spacing` と `margin-inline-end` の両方から参照する形になる。単純に見える演出が型付きカスタムプロパティを要求する例。

## F008 stagger はマークアップに番号を書かずに CSS だけで書ける

- **主張** — `animation-delay: calc((sibling-index() - 1) * 110ms)` で順送りが成立する。各要素に `--i` を振る必要がない。
- **根拠** — `experiments/stagger-index` と `experiments/stagger-head` の計算済み `animation-delay` を比較。前者（CSS のみ）と後者（`--i` を手書き）がいずれも `0s / 0.11s / 0.22s / 0.33s / 0.44s` で完全に一致。`CSS.supports()` も true。
- **確度** — 実測（Chromium）
- **効き方** — stagger を再利用可能なパーツとして配れるようになる。これまでは要素数ぶんの `nth-child` を書くか、生成側で `--i` を埋めるしかなく、それが stagger をライブラリ化できない主因だった。要素数に依存しない書き方が可能になったことで、L3 の編成パターンをそのまま配布できる。
- **ブラウザ実測（2026-08-02）** — Safari 26.5 では対応、Firefox 153 では未対応。未対応環境では `calc()` 全体が無効になり遅延0（全部同時）に落ちるため、劣化は静かで気づきにくい。

## F009 同時・時差・別イージングの違いは知覚できる

- **主張** — 同じ2つの動き（位置と不透明度）を、同時に動かすか・時差をつけるか・別々のイージングを当てるかの違いは、見て区別がつく。
- **根拠** — `experiments/sim` / `lag` / `split-ease` を並べての判定。
- **確度** — 実測（人による判定）
- **効き方** — 「何を動かすか」だけでなく「どう重ねるか」が独立した設計軸として成立する。原子の組み合わせを列挙するより、この重ね方の型を整理するほうが得られるものが大きい。

## F010 stagger の「まとまり」と「個別」の境目は 150ms 付近（暫定）

- **主張** — 5 要素の順送りで、間隔が 150ms になると個々が独立して見え始める。110ms 以下ではひとまとまりとして読める。
- **根拠** — `experiments/stagger-head`(110ms) / `stagger-dense`(45ms) / `stagger-sparse`(150ms) の判定。
- **確度** — **暫定**。境目が間隔の絶対値なのか、間隔÷長さの重なり率なのかを、まだ分離できていない。
- **経過** — 長さを 400ms に固定した追試（`gap-40` / `gap-80` / `gap-120` / `gap-150`）でも、150ms（比率 0.38）が「ぎりぎり個別」。ただしこの追試も長さを 380ms→400ms としか変えておらず、絶対値と比率のどちらが効いているかは依然として分離できない。**間隔を固定して長さを大きく振る**設計でなければ判別できない。同じ間違いを 2 度繰り返した。
- **分かっていること** — 間隔 110〜120ms は「まとまり」、150ms で「個別」に切り替わる。長さが 380〜520ms の範囲では、この境目は動かない。

## F011 親子を逆方向に動かすだけでは奥行きにならない

- **主張** — 親が上がり中身が逆に下がる動きは、それだけでは視差として読めず、単にずれて見える。奥行きの成立には動き以外の手がかりが要る。
- **根拠** — `experiments/counter` の判定。「奥には見えない」「カードの質感だと思う」。
- **確度** — 実測（人による判定）
- **効き方** — 視差を動きの型として配ることはできない。面・縁・影・内容といった「そこに物がある」ことを示す手がかりとセットで初めて成立する。動きの設計と質感の設計が分離できない領域がある、ということ。
- **追試** — 影 / 遠近 / 被写界深度 / カードの質感 のいずれを足しても奥行きにはならなかった。ただしこの追試は F012 の汚染を受けており、結論として扱えない。

## F012 被写体の見た目そのものが判定を汚染する

- **主張** — 既定の被写体（角丸の四角に斜めのグラデーション）は「膨らんだ物体」として読まれる。この見た目が持つ意味が、動きの評価に混入する。
- **根拠** — 奥行きの手がかりを 4 通り足した追試が全滅した際の指摘。「このカード自体が膨らんでる色してるんだよね。だからどれも奥行きになってない」。手前に出ようとしている物体の上では、どんな奥行き表現も成立しない。
- **確度** — 実測（人による判定）
- **効き方** — 下地は動きから独立した軸として扱わなければならない。`b` キーで グラデ / 単色 / 白い面 / 輪郭のみ を切り替えられるようにした。今後、質感が絡む判定は必ず複数の下地で見る。**これ以前に取った判定はすべてグラデーション下地のもの**として読む必要がある。

## F034 `rotateY(180deg)` の裏面は、非対称な指定が反転する

- **主張** — 裏返した面に書く値は「画面で見たい形」ではなく「反転された結果がそうなる形」でなければならない。角丸・影の向き・グラデーションの方向・非対称な余白がすべて対象。
- **根拠** — `experiments/page-turn`。表面と裏面に同じ `border-radius: 4px 8px 8px 4px` を書いたところ、裏面は左右が入れ替わって綴じ側が 8px、外側が 4px になった。表から裏へ切り替わる瞬間に丸みが反対側へ飛ぶ。裏面に反転済みの `8px 4px 4px 8px` を書くと画面上で揃う。
- **確度** — 実測
- **効き方** — 対称な指定（一律の角丸、中心対称の影）しか持たない面では起きないため、気づかないまま設計が進む。非対称な装飾を足した時点で初めて表面化する。

## F033 回転は「角度」ではなく「軸と、動いた先に何があるか」で決まる

- **主張** — 回転する動きは、角度と見た目を合わせるだけでは成立しない。回転の軸をどこに置き、回った先に何が待っているかまで設計する必要がある。
- **根拠** — 3 件が同じ理由で崩れた。`experiments/page-turn` は葉と「現れるページ」を同じ側に置いたため、めくった葉が現れるはずのページを覆った（実測で終端の葉と次頁が完全に重なる）。`experiments/rig-chain` は関節の間隔を `margin` で取り、相殺されて 5 段が同じ位置に重なった。`experiments/origami` は開いた形が舞台からはみ出した。
- **確度** — 実測
- **効き方** — 3D の動きを作るときは、静止した始点と終点の**両方**を先に置いてから、間を回転で埋める。角度から始めると終点が設計されない。

## F032 個体差の作り方は表現によって選ぶ

- **主張** — 複数の要素に違いを付ける方法は 2 通りあり、表現によってどちらかしか使えない。**位相差が表現の本体であるもの**（波・隊列・群衆・流れ）は負の遅延で作る。**同時に起きることが表現の本体であるもの**（停止・衝撃・一斉表示）は遅延を使わず、振幅や距離で違いを作る。
- **根拠** — `experiments/time-stop`。他の実験で有効だった「負の遅延で個体差を作る」をそのまま持ち込んだところ、停止区間も要素ごとに別の時刻へずれ、実測で停止中の脇役が動き続けていた（t=450 で 5 番目だけ 12px ずれ、t=600 では全員がばらばら）。遅延をやめて縦の振れ幅を 3 通りにしたところ、停止区間の 3 時点で位置が完全に一致した。
- **確度** — 実測
- **効き方** — F021（負の遅延には `infinite` が要る）と組みで覚える必要がある。負の遅延は強力だが、**時間軸を共有しなければ成立しない表現には使えない**。

## F031 `pathLength` は `non-scaling-stroke` と併用すると効かない

- **主張** — `pathLength` による長さの正規化は `vector-effect="non-scaling-stroke"` と併用すると無効になる。破線の長さがデバイス座標で解釈されるため、`stroke-dasharray` に書いた値と実際の描画長が一致しなくなる。
- **根拠** — `experiments/eye-path`。`pathLength="100"` かつ `stroke-dasharray: 100` としたのに、`getTotalLength()` は 153 を返し、画面上の長さは約 296px。`dasharray: 100px` がデバイス座標として扱われ、線の 3 分の 1 しか描けなかった。`non-scaling-stroke` を外すと正しく全長が描かれる。
- **確度** — 実測（Chromium）
- **効き方** — **より重要なのは、なぜ `non-scaling-stroke` を足したかのほう。** SVG と HTML で座標系が 2 つになったのを `preserveAspectRatio: none` の伸縮で合わせ、伸縮で線が歪むのを `non-scaling-stroke` で補い、それが破線を壊した。**対策が次の不具合を生む連鎖**で、正しい手当ては最初の判断（座標系を混ぜたこと）を戻すことだった。点も箱も SVG の中に入れれば、伸縮も補正も要らない。

## F030 `scroll-state` コンテナ問い合わせは解釈されるが評価されない

- **主張** — `container-type: scroll-state` と `@container scroll-state(...)` は構文として受け付けられるのに、条件が満たされても中身が適用されない。
- **根拠** — 最小の再現を作って確認した。スクロール領域の中に `position: sticky; top: 0; container-type: scroll-state` の要素を置き、`scroll-state(stuck: top)` と `scroll-state(scrollable: top)` の両方で子の見た目を変える規則を書いた。要素は実際に貼り付いており（上端から 1px）、スクロール量も 100px あるが、子の高さも背景も変わらない。規則は `CSSStyleSheet.cssRules` に入り、`CSS.supports('container-type','scroll-state')` は `true` を返す。同じ文書で `@container style()` は機能しているため、コンテナ問い合わせ全体ではなく `scroll-state` だけが不活性。
- **確度** — 実測（Chromium）。ただし後述の留保あり
- **効き方** — F023（`::marker`）F028（`d`）と同じ「指定は通るが効かない」型で、これで 4 件目。**構文が通ることと機能することは別**という前提で使う必要がある。貼り付きに応じた見出しの変化は、いまのところ `animation-timeline: scroll()` に範囲を指定して作るのが現実的。
- **留保** — この測定環境は描画が止まっており、スクロール駆動アニメーションも `currentTime` が `null` になる。`scroll-state` の不活性が描画停止の副作用である可能性を排除できていない。通常に描画しているブラウザでの再確認が要る。

## F029 入れ物より大きい要素は `place-items: center` で中央に来ない

- **主張** — グリッドの中央寄せは、要素が入れ物より大きいと効かない。要素の左上が入れ物の左上に一致する形で置かれる。
- **根拠** — `experiments/god-rays`。115px 幅の入れ物に 260px の要素を `place-items: center` で置いたところ、中心のずれが実測で **(72, 71)px**。これは「要素の左上が入れ物の左上に一致」した場合の値と一致する。`speed-lines`（260px）と `impact-frame`（150px）も同じ状態だった。
- **確度** — 実測（Chromium）
- **効き方** — 集中線・光条・波紋のように**わざと画面外まではみ出させる**表現は、ほぼ必ずこの条件に当たる。しかも入れ物が十分広いうちは正しく中央に見えるので、**画面が狭くなって初めて崩れる**。気づくのが遅れる類の不具合。
- **回避** — `position: absolute` と `left/top: 50%` に、大きさの半分の負の `margin` を当てる。`translate: -50% -50%` でも中央には来るが、`translate` は動きに使うことが多く奪い合いになる。`margin` なら動きの記述と衝突しない。

## F028 `d` は `<path>` にしか効かない ── そして計算値では効いたかどうか判定できない

- **主張** — CSS の `d` プロパティは `<path>` 専用。`<circle>` や `<rect>` に指定しても計算値には現れるが、描画は一切変わらない。
- **根拠** — `experiments/boil`。`<circle>` に対して `d: path(...)` を 3 状態でアニメーションさせたところ、`getComputedStyle(el).d` は 3 つの異なるパス文字列を返した。しかし輪郭上の点を `getPointAtLength` で実測すると、3 状態の**最大移動量は 0px**。`<path>` に変えると 8.4px（線幅の 2.8 倍）動くようになった。
- **確度** — 実測（Chromium）
- **効き方** — F023（`::marker`）と同じ形。**指定が通ることは、効くことを意味しない。** SVG の図形要素はそれぞれ固有の幾何プロパティ（`r` / `cx` / `width`）を持ち、`d` を受け付けるのは `<path>` だけ。形を補間したいなら最初から `<path>` で書く。
- **検証方法への教訓** — このとき私は「計算値が 3 つに変わっているから動いている」と判断し、**間違った**。計算値はスタイルが適用されたことしか示さず、描画に反映されたかは示さない。スタイルの検証は計算値ではなく、**形や位置の実測**で行う必要がある。`getBBox()` や `getPointAtLength()` のような幾何を返す API、あるいは要素の矩形を測るのが正しい。

## F027 `overflow: hidden` が `scroll(nearest)` を横取りする

- **主張** — `overflow: hidden` はスクロール領域を作る。途中の要素にこれが付いていると、`scroll(nearest)` はそこを拾ってしまう。その要素は決してスクロールしないので、進行度が出ず何も動かない。
- **根拠** — `experiments/scroll-marquee`。外側のスクロール領域を意図していたが、`overflow: hidden` を持つ帯が最も近い領域として選ばれていた。タイムラインの供給元が `.band` に解決されており、`currentTime` は常に無効。名前付きのタイムライン（`scroll-timeline: --smq` / `animation-timeline: --smq`）に変えると供給元が意図した要素になった。
- **確度** — 実測
- **効き方** — はみ出しを隠すために置いた `overflow: hidden` が、無関係に見えるスクロール駆動を壊す。**エラーも警告も出ず、ただ動かない。** 中間要素に切り抜きがある構造では `scroll(nearest)` を使わず、名前で明示するほうが安全。切り抜きだけが目的なら `overflow: clip` はスクロール領域を作らないので、こちらでも回避できる。
- **測り方の注意** — 描画が止まっている環境では、正常なスクロール駆動でも `currentTime` が `null` になる。不具合かどうかは進行度ではなく**タイムラインの供給元**で判別する。

## F026 sticky は自分の親の範囲でしか止まらない

- **主張** — 貼り付いた見出しを積み上げたいなら、見出しはスクロール領域の直下に並んでいる必要がある。節ごとの入れ物に入れると、その節が過ぎた時点で見出しも一緒に流れ去る。
- **根拠** — `experiments/sticky-stack`。節を `div` で包んだ構造では、先頭の見出しがスクロール中に `y = -20` まで流れて消えた。入れ物をやめて見出しと本文を同じ親の直下に並べたところ、3 本とも `0 / 22 / 44px` に残るようになった。
- **確度** — 実測
- **効き方** — 「見出しが上に残る」という見た目は、構造の制約とセットでしか成立しない。装飾のために節を包むと壊れるので、包む必要があるなら sticky を別の階層へ出す。
- **併発した誤り** — 段数の指定に `:nth-of-type()` を使ったが、これは**同じ要素型の中で何番目か**を数えるもので、クラスでは絞られない。見出しと本文がどちらも `div` だったため 2 本目以降に当たっていなかった。並び順から出すなら `sibling-index()` のほうが誤りにくい。

## F025 `content-visibility: auto` の配下は `getAnimations({subtree:true})` から漏れる

- **主張** — 描画が省かれている部分木は `getAnimations({subtree: true})` の走査対象から外れる。要素を直接問い合わせればアニメーションは取れるので、消えているのは走査であってアニメーションではない。
- **根拠** — `experiments/content-vis`。10 件のうち 5 件に `content-visibility: auto` を付けた状態で、親からの `getAnimations({subtree:true})` は 5 件しか返さない。一方、各要素に直接 `getAnimations()` を呼ぶと 10 件すべてが 1 本ずつ持っている。
- **確度** — 実測（Chromium）
- **効き方** — アニメーションを走査して制御する道具を書くとき、**取りこぼしが起きても例外にならない**。この研究環境の時計も部分木の走査で駆動しているので、長い一覧に `content-visibility` を使った実験は時計が届かない可能性がある。省略された部分を数に入れたいなら、要素を個別に辿る必要がある。

## F024 `::first-letter` は宣言ごと消える（`::marker` とは失敗の仕方が違う）

- **主張** — `::first-letter` に `animation-name` を書いても、計算値が `none` になる。指定そのものが受け付けられない。
- **根拠** — `experiments/first-letter`。`getComputedStyle(p, '::first-letter').animationName` が `none`。一方 `::marker` は同じ指定が計算値としては残る（F023）。
- **確度** — 実測（Chromium）
- **効き方** — **同じ「擬似要素を動かしたい」でも失敗の仕方が 2 通りある。** `::marker` は指定が残るのにアニメーションが生成されず、`::first-letter` は指定自体が消える。前者は開発者ツールで宣言が見えるぶん余計に紛らわしい。どちらも回避は「実要素で置き換える／包む」で共通。擬似要素は見た目の指定には使えても、動かす対象としては当てにできない。

## F023 `::marker` にはアニメーションが効かない（宣言は通る）

- **主張** — `::marker` に `animation-name` を指定すると計算値としては適用されるが、アニメーションそのものは生成されない。エラーにも警告にもならず、ただ動かない。
- **根拠** — `experiments/marker-anim`。`getComputedStyle(li, '::marker').animationName` は `exp-marker-anim` を返すのに、`getAnimations({subtree:true})` に `::marker` のものは 1 件も現れない。同じセルの `::before` は 3 件生成される。
- **確度** — 実測（Chromium）
- **効き方** — 箇条書きのマーカーを動かしたい場合は `list-style: none` にして `::before` を自前で置くしかない。宣言が受け入れられるため「書いたのに動かない」の原因に辿り着きにくい。指定が通ることは、動くことを意味しない。

## F022 `animation-iteration-composite: accumulate` は使えない

- **主張** — 繰り返しのたびに前回の結果へ積み上げる指定は、Chromium では未対応。指定は無視され、毎回初期値に戻る。
- **根拠** — `experiments/iter-accumulate`。`CSS.supports('animation-iteration-composite', 'accumulate')` が false。90° を 4 回繰り返して 360° にする書き方は成立せず、90° の跳ねが 4 回続くだけになる。
- **確度** — 実測（Chromium）
- **効き方** — 「1 回ぶんの量」と「1 回にかける時間」を分けて指定する手段が今は無い。総量をキーフレームに直接書くしかないため、速度だけを変えたいときも角度の記述を触ることになる。実験は否定的結果として残し、動く書き方を並置してある。

## F021 負の遅延で位相をずらすには `animation-iteration-count: infinite` が要る

- **主張** — 要素ごとに負の遅延を与えて位相をずらす手法は、繰り返しが 1 回のままだと成立しない。遅延が大きい要素ほど早く終端に達し、`fill-mode` によってそこで止まる。閉じた動きでは終端と始点が同じ位置なので、止まった順に全員が 1 点へ溜まっていく。
- **根拠** — `experiments/orbit-train`。8 個を 1 周期の 1/8 ずつずらしたが「等間隔で流れず 1 点に集まる」。同じ原因で `gooey` `ripple` `pulse-badge` `wave-text` も終端で潰れていた。`animation-iteration-count: infinite` を足すと位相が剰余で回り続け、全周にわたって等間隔を保つ。
- **確度** — 実測
- **効き方** — 「同じアニメーションに負の遅延を段階的に与えるだけで隊列ができる」という手法は強力だが、**無限繰り返しとセットでないと使えない**。逆に、正の遅延で順送りにする場合は 1 回で正しく、両者で必要な設定が違う。この非対称は見落としやすい。

## F017 継ぎ目のない流れに必要な複製枚数は「コンテナ幅 ＋ 1 周期」

- **主張** — 内容を並べて 1 周期ぶん動かす流れは、複製 2 枚では足りない。1 周期動いた時点で末尾が表示窓に届かず、右端に空白が出る。
- **根拠** — `experiments/marquee`。コンテナ幅 230px、1 周期 200px、複製 2 枚（総幅 400px）の場合、200px 動かした時点で表示窓は 200〜430px を映すが中身は 400px で終わる。差の 30px が空く。4 枚（総幅 800px）にして 25%（＝1 周期）動かすと解消した。
- **確度** — 実測
- **効き方** — 必要条件は **複製の総幅 − 1 周期 ≧ コンテナ幅**。「2 枚並べて -50%」という広く出回っている書き方は、1 周期がコンテナ幅以上のときしか成立しない。可変幅で使うなら枚数に余裕を持たせるか、1 周期をコンテナ幅以上にする。

## F018 回転を混ぜると柔らかさが消える

- **主張** — 輪郭を変形させる動きに回転を足すと、中身ごと回るため「柔らかい塊」ではなく「回転する固体」に見える。
- **根拠** — `experiments/blob`。`border-radius` の 8 値を動かしつつ `rotate: 360deg` を併用した版は「液体っぽくない」と判定された。回転を外して非一様な `scale` に置き換えると柔らかさが出た。
- **確度** — 実測（人による判定）
- **効き方** — 変形を見せたいときは、要素そのものを回さない。どうしても回転が要るなら、中身を逆回転させて相殺する必要がある。あわせて、`border-radius` の変形だけでは「柔らかい塊」止まりで**液体には見えない**。液体感は `gooey`（ぼかし → 高コントラスト）の融合でしか出ない。

## F019 対称形なら継ぎ目を保ったまま速度を落とせる

- **主張** — 回転する要素が n 回対称なら、1 周を 360°/n にしても見た目は繋がる。速度を n 分の 1 にできる。
- **根拠** — `experiments/liquid`。角丸を一律 44% にした正方形は 4 回対称。`rotate: 360deg` が「速すぎる」と判定されたため `90deg` に変更し、継ぎ目を出さずに 1/4 の速さにした。
- **確度** — 実測
- **効き方** — 尺が固定された仕組みの中で速度を調整する手段になる。逆に言えば、非対称な形を回すときは 1 周ぶん回すしかなく、速度は尺で決まってしまう。
- **振幅の決まり方** — この手法の波の大きさは 2 つで決まる。**角丸が小さいほど**辺の直線部分が長くなり、回したときの高低差が大きくなる。**塊が窓に対して小さいほど**、見えている範囲での曲がりが強くなる。塊を大きくすると水平線に近づいて波が消える。加えて、逆回転させた 2 層目を重ねると位相が干渉し、単調な上下動に見えなくなる。逆回転も 4 回対称なら −90° で継ぎ目が保たれる。

## F020 「何が起きているか」は文脈がないと伝わらない

- **主張** — 動き自体が正しくても、周囲に手がかりがないと意図した現象として読まれない。
- **根拠** — `experiments/mask-hole`。覆いに穴を開けて動かす技法だが、下地を無地のグラデーション、覆いを背景と同色にしていたため「ただ丸が動いているだけ」と判定された。下に中身（番号付きのタイル）を敷き、覆いに目地を入れて板と分かるようにしたところ、穴として読めるようになった。
- **確度** — 実測（人による判定）
- **効き方** — F012（下地が判定を汚染する）の続き。技法を見せる実験では、**その技法が何をしているかを示す最小限の情景**まで含めて設計する必要がある。裸の四角の上では成立しない技法がある。

## F015 用途を UI 部品の単位で固定しても、まだ答えは決まらない

- **主張** — 「モーダル」「トースト」まで場面を絞っても優劣は決まらない。決めているのは部品の種類ではなく、その場面が持つ **注意度・由来・反復耐性** のほう。
- **根拠** — 「用途による」が 3 階層すべてで返ってきた。原子（`spin` の回転量）、合成（`lag` の時差量）、レシピ（モーダル・トースト）。いずれも差は知覚できているのに優劣が決まらない。一方で見出しの `head-chars` だけは明確に支持された。見出しは繰り返し見ないため、反復耐性の低い型を使えることで説明がつく。
- **確度** — 実測（人による判定）
- **効き方** — 成果物の形を変える必要がある。「この用途にはこの型」という対応表は原理的に書けない。書けるのは **型ごとの性格タグ**であり、選ぶのは使う側。[PATTERNS.md](PATTERNS.md) をこの形で起こした。ライブラリや MCP として出すときも、返すべきは「正解の 1 つ」ではなく「性格で絞り込める一覧」になる。

## F016 リストの高さを開く手段は grid-template-rows で決着

- **主張** — `grid-template-rows: 0fr → 1fr` と `interpolate-size: allow-keywords` + `height: auto` は、見た目に差が出ない。差がない以上、対応範囲の広い前者を採る。
- **根拠** — `experiments/list-grid` と `experiments/list-size` の比較判定。「差がない」。到達点はいずれも 0px → 20px で一致。
- **確度** — 実測
- **効き方** — 「`height: auto` へは遷移できない」という長年の制約に対する実用上の答えが 1 つに定まった。`interpolate-size` は書き方が素直だが、それだけでは選ぶ理由にならない。

## F014 `document.getAnimations()` は CSSTransition も返す

- **主張** — `document.getAnimations()` の戻り値には `CSSAnimation` と `CSSTransition` が混在する。両者を区別せず `currentTime` を代入すると、遷移が代入先の時刻まで一気に飛ばされ、一瞬で終わる。
- **根拠** — L4 のレシピが全件「一瞬で変化する」状態になっていた。実測すると全 112 件のうち 100 件が `CSSAnimation`、12 件が `CSSTransition`。時計は 200〜380ms の遷移に対して毎フレーム 1000ms を代入していた。`CSSAnimation` だけが `animationName` を持つのでこれで判別できる。
- **確度** — 実測
- **効き方** — アニメーションを外から駆動する道具を作るときは、必ず両者を分けて扱う。時間軸を持たない遷移を時間軸で駆動してはならない。
- **検証方法の教訓** — このバグは「遷移を無効化して到達点の計算値を読む」という検証をすり抜けた。**到達点が正しいことは、そこへ至る過程が正しいことを保証しない。** 動きの検証には、途中の時刻での値か、実際に見た目を確認する手段が要る。

## F013 用途を固定しない比較は成立しない

- **主張** — 「A と B のどちらが良いか」を用途抜きで訊いても答えは出ない。出るのは「用途による」だけ。
- **根拠** — 2 回繰り返した。`spin` の回転量（「この程度のものは用途による。両方正解」）、`lag` の時差量（「わからない。用途によるでしょう」）。いずれも差は知覚できているのに、優劣が決まらない。
- **確度** — 実測（人による判定）
- **効き方** — 型の優劣を単体で決めようとする実験設計が誤り。**先に用途（モーダル、リスト、トースト、見出し）を固定し、その中で型を比較する**。抽象の階段を下から積む順序ではなく、用途を先に置いて必要な型を引く順序にする。判定してもらう問いは「どちらが良いか」ではなく「この用途にどれが合うか」でなければならない。
