# 参加のしかた

このリポジトリで足りていないのは実験の数ではない。**判定**のほうが足りていない。

211 件のうち、人の目で判定されたものはまだごくわずかで、
「採用 CSS」（[dist/adopted.css](dist/adopted.css)）は空に近い。
理由は単純で、211 件を 1 人で見るのは無理だからだ。
そして仮に 1 人で見切ったとしても、それは 1 人の好みでしかない。

1 人が 20 件見るなら現実的な量になる。だから判定を分けたい。

## 判定を送る

1. <https://cssanime.yhay81.com/lab/contact.html> を開く
2. 気になったものに判定を付ける（`p` 採用 / `x` 却下 / `f` 気になる / `s` 手本）
   - 全部を見る必要はない。**20 件でも十分意味がある**
   - 判定は端末の中にだけ保存される。どこにも送信されない
3. 「判定を書き出す」で `my-verdicts.json` を落とす
4. `verdicts/<あなたの名前>.json` として置き、プルリクエストを送る

判定するとき、条件（イージング・被写体・速さ）を変えたなら、そのまま送ってよい。
判定と一緒に条件も記録される。**同じ動きでも条件が違えば別の判定になる**ことのほうが、
情報として価値がある。

### 判定の意味

| 判定 | 意味 |
|---|---|
| 採用 `p` | 使える。場面が合えば選ぶ |
| 却下 `x` | 使えない。または他に劣る |
| 気になる `f` | 良し悪しは決めきれないが、何かある |
| 手本 `s` | これを基準にしたい。`dist/adopted.css` に入る候補 |

**迷ったら「気になる」を使ってほしい。** 決めきれなかったこと自体が記録に値する。

## 何が起きるか

送られた判定は [scripts/merge-verdicts.mjs](scripts/merge-verdicts.mjs) で突き合わされ、
`dist/consensus.json` になる。ここで見ているのは多数決ではなく、**意見が割れた実験**のほう。

全員が同じ判定を付けた実験は、もう見なくていい。
割れた実験には、まだ言語化されていない基準が埋まっている。
[PATTERNS.md](PATTERNS.md) の 3 軸（注意度・由来・反復耐性）は、そうやって割れを追って出てきたもの。

`dist/adopted.css` に入るのは維持者が `手本` を付けたものだけで、この規則は変えない。
多数決で採用を決めると、平均的で無難なものだけが残る。

## 実験を足す

`experiments/<id>/` に 2 つ置く。

```text
experiments/<id>/meta.json
experiments/<id>/anim.css
```

`anim.css` の通常セレクタは `[data-exp="<id>"]` の下へ閉じる。
必須の軸は層で変わる（[AGENTS.md](AGENTS.md) 参照）。

出す前に:

```bash
npm test
npm run validate
npm run export
```

## 知見を足す

[FINDINGS.md](FINDINGS.md) は、このリポジトリで最も価値のある部分。
**効かなかったことを、効いたことと同じだけ書く**。

形式は固定する。

- **主張** — 一文で言い切る
- **根拠** — どの実験で確かめたか
- **確度** — `実測` / `仕様から確定` / `仮説` のいずれか
- **効き方** — この知見が実際に何を変えるか

確かめていないものを `実測` と書かないこと。ここが崩れると全体が信用できなくなる。

## 検査規則を足す

「エラーにならないまま黙って壊れる」ものを踏んだら、それは規則にする価値がある。

- 文字列だけで判定できるなら [scripts/checks/static.mjs](scripts/checks/static.mjs)
- 実際に動かさないと分からないなら [scripts/checks/runtime.mjs](scripts/checks/runtime.mjs)

規則には必ず、**壊れた入力で発火することの試験**を
[test/static-checks.test.mjs](test/static-checks.test.mjs) に添える。
無指摘のカタログだけでは、検査が常に空を返していても気づけない。

## ブラウザ別の実測を送る

<https://cssanime.yhay81.com/lab/verify.html> を手元のブラウザで開き、
「結果JSON」を書き出して [browser-support.json](browser-support.json) に足す。
Chromium 以外の実測が特に足りていない。
