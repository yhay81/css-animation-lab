# 型と性格の対応表

「モーダルにはこの動き」という形では書けない。3 階層すべてで「用途による」という答えが返ってきた
（[FINDINGS.md](FINDINGS.md) の F013・F015）。決めているのは UI 部品の種類ではなく、その場面が
持つ性質のほうだった。

そこでこの表は**優劣を書かない**。型ごとの性格を書き、選ぶのは使う側に任せる。

## 決定の軸

| 軸 | 平たく言うと | 見て確かめる方法 |
|---|---|---|
| **注意度** | 一覧を眺めたとき、どれに先に目が行くか | 一覧を数秒眺めて、最初に目が行くものを挙げる |
| **由来** | 「どこから出てきたか」が動きから分かるか | 動きが方向を持つかどうか。機構から決まるので判定は要らない |
| **反復耐性** | 同じ動きが何度も繰り返されたとき、うるさく感じないか | 一覧の「連打」を入れて数秒見る。休みなしで繰り返される |

使い方はこうなる。注意度が高い型は、めったに出ないものに使う。反復耐性が低い型は、
1 セッションに何度も出るものには使えない。由来を示す型は、出どころを分からせたいときに使う。

**反復耐性は場面の側の性質と掛け合わせる。** 見出しは 1 回しか見ないので反復耐性が低い型を使える。
同じ型をトーストに使うと過剰になる。`head-chars` が支持されて `toast-pop` が決め手を欠いたのは、
おそらくこの差。

## 型の性格

確度の列は、判定で裏が取れているか（**判定済**）、機構から導いたものか（**提案**）を示す。

| 型 | 注意度 | 由来 | 反復耐性 | 確度 |
|---|---|---|---|---|
| `fade` その場で不透明度だけ | 低 | 示さない | 高 | 提案 |
| `rise` / `slide` 方向を持つ移動 | 中 | **示す** | 中 | 提案 |
| `pop` 拡大登場 | 中 | 示さない | 中 | 提案 |
| `shrink` 縮小登場 | 中 | 示さない | 中 | 判定済（`pop` と同格・別用途） |
| `wipe` / `mask-sweep` 露出 | 中 | 示さない | 高 | 提案 |
| `blur` ぼかしの解除 | 中 | 示さない | 低 | 提案（負荷も高い） |
| `overshoot` 行き過ぎて収まる | **高** | 示さない | **低** | 提案 |
| `spin` 回転 | 量による | 示さない | 量による | 判定済（「両方正解」） |
| `stagger` 順送り | 中 | 順序を示す | 中 | 判定済（間隔で変わる） |
| `counter` 親子逆方向 | — | — | — | 判定済（**単体では意味を持たない**） |

## 合成の型

差は知覚できるが優劣はない（F009）。性格の差として使う。

| 型 | 効果 |
|---|---|
| `sim` 位置と不透明度を同時 | 素直。基準線 |
| `lag` 不透明度が先、位置が遅れて | 到達が重くなる。落ち着いた印象 |
| `split-ease` 位置は減速、不透明度は等速 | 動きは滑らかなまま、存在の立ち上がりだけ均一になる |
| `add` 加算合成 | 性格ではなく機構。2 本の同一プロパティを後勝ちさせずに足せる |

## stagger の間隔

判定は速さ 0.9 倍のときのものなので、**実時間に換算した値**で記す。

| 実時間の間隔 | 見え方 |
|---|---|
| 〜約 108ms | ひとまとまりとして読める |
| 約 135ms | ぎりぎり個別に見え始める |

長さ 340〜470ms の範囲では、この境目は動かなかった。間隔の絶対値と重なり率のどちらが効いているかは
未分離（F010）。**ひとまとまりに見せたいなら 100ms 前後、一つずつ数えさせたいなら 140ms 以上**、
という運用ならこのままで足りる。

## 決着した選択

| 問い | 結論 | 根拠 |
|---|---|---|
| リストの高さを開く手段 | **`grid-template-rows: 0fr → 1fr`** | 見た目に差がなく、`interpolate-size` より対応範囲が広い |
| 見出しの登場 | **文字ごとの順送り**（`sibling-index()`） | 判定で支持。見出しは反復されないので反復耐性の低さが問題にならない |

## 決着しなかった選択

| 問い | 状態 |
|---|---|
| モーダルの開き方 | 優劣つかず。その場で開く＝注意度低・由来なし、下から持ち上げ＝注意度中・由来あり、として性格で選ぶ |
| トーストの出し方 | 優劣つかず。横から＝反復向き、下から跳ね＝稀な通知向き、として性格で選ぶ |

これらは「決められなかった」のではなく、**単一の正解が存在しない**という結果として扱う。
